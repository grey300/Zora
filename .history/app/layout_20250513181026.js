import { Inter } from "next/font/google";
// import "./globals.css";
import { ClerkProvider, GoogleOneTap } from "@clerk/nextjs";
import { ReactQueryProvider } from "@/providers/ReactQueryProvider";
import "./_components/styles/globals.css";
import Layout from "@/app/_components/components/Layout";
const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

export const metadata = {
  title: "Zora",
  description: "Generated by create next app",
};

export default function RootLayout({ children }) {
  return (
    <ClerkProvider
      signInUrl="/sign-in"
      signUpUrl="/sign-up"
      navigateAfterSignIn="/dashboard"
      navigateAfterSignUp="/dashboard"
      clockSkewInSeconds={60}
    >
      <html lang="en">
        <body className={`${inter.variable} antialiased `  className="bg-gray-900"}>
          <ReactQueryProvider>
            <Layout>
              {" "}
              {/* Wrap children with Layout */}
              {children}
            </Layout>
            <GoogleOneTap />
          </ReactQueryProvider>
        </body>
      </html>
    </ClerkProvider>
  );
}
