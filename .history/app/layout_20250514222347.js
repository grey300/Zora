import { Inter } from "next/font/google";
import "./globals.css";
import { ClerkProvider } from "@clerk/nextjs"; // Remove GoogleOneTap import for now
import { ReactQueryProvider } from "@/providers/ReactQueryProvider";
import Layout from "@/app/_components/components/Layout";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

export const metadata = {
  title: "Zora",
  description: "Generated by create next app",
};

export default function RootLayout({ children }) {
  return (
    <ClerkProvider
      signInUrl="/sign-in"
      signUpUrl="/sign-up"
      navigateAfterSignIn="/dashboard"
      navigateAfterSignUp="/dashboard"
      clockSkewInSeconds={60}
      debug={true}
    >
      <html lang="en">
        <body className={`${inter.variable} antialiased`}>
          <ReactQueryProvider>
            <Layout>{children}</Layout>
            {/* Temporarily remove GoogleOneTap to isolate the issue */}
            {/* <GoogleOneTap /> */}
          </ReactQueryProvider>
        </body>
      </html>
    </ClerkProvider>
  );
}
